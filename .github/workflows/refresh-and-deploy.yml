name: Refresh data and deploy
on:
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"
permissions:
  contents: write
  pages: write
  id-token: write
concurrency:
  group: workforce-observatory
  cancel-in-progress: false
jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Refresh public indicators
        run: python scripts/refresh.py
      - name: Sync backend-managed content
        if: ${{ secrets.BACKEND_ORIGIN != '' && secrets.BACKEND_SYNC_TOKEN != '' }}
        env:
          BACKEND_ORIGIN: ${{ secrets.BACKEND_ORIGIN }}
          BACKEND_SYNC_TOKEN: ${{ secrets.BACKEND_SYNC_TOKEN }}
        run: python scripts/sync_backend.py
      - name: Validate generated JSON
        run: python -m json.tool data/dashboard.json > /dev/null
      - name: Commit refreshed data
        run: |
          git config user.name "workforce-observatory-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/dashboard.json
          git diff --cached --quiet || git commit -m "data: refresh public workforce indicators"
          git push
      - name: Build scheduled Workforce Observatory site
        run: python scripts/build.py
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
